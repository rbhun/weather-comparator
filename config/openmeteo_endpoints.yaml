modes:
  analysis:
    endpoint: "https://customer-archive-api.open-meteo.com/v1/archive"
    auth_mode: "require_key"
  previous_runs:
    endpoint: "https://customer-previous-runs-api.open-meteo.com/v1/forecast"
    auth_mode: "require_key"
  live:
    endpoint: "https://customer-api.open-meteo.com/v1/forecast"
    auth_mode: "require_key"

